<?php

// Вивід помилок на екран

// Встановлення значення налаштування конфігурації. Налаштування буде зберігати встановлене значення, поки виконується скрипт. Після завершення роботи скрипта значення налаштування повернеться до вихідного.
ini_set('display_errors', 1);

// Активація звіту про помилки. Вивести warnings, errors, notices.
error_reporting(E_ALL);

// Функція для дебага. Можна ще print_r.
function debug($str) {
    echo '<pre>';
    var_dump($str);
    echo '</pre>';
    exit;
}